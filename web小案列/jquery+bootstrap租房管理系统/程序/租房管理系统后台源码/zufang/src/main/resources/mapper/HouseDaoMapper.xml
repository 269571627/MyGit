<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wl.zufang.dao.HouseDao">
    
    <select id="getById" parameterType="long" resultType="com.wl.zufang.entity.House">
        select * from house where id = #{id} and state = '0'
    </select>
    
    <update id="updateRent" parameterType="long">
        update house set state = '1' where id = #{id}
    </update>
    
    <update id="updateRetreat" parameterType="long">
        update house set state = '0' where id = #{id}
    </update>
    
    <select id="queryAll" resultType="com.wl.zufang.entity.House">
        select * from house
    </select>
</mapper>